# API Configuration
# In development, Vite proxy handles this (see vite.config.ts)
# In production, set this to your Railway backend URL
VITE_API_URL=http://localhost:3001

# Add other frontend env vars here as needed
